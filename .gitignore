# direnv
/.envrc

# NOTE(azazeal): Neovim keeps its runtime state (plugins, shada, undo history) under
# ~/.local/share/mivn and ~/.local/state/mivn, outside this repo, so there is
# nothing of it to ignore here.
